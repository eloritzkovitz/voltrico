apiVersion: apps/v1
kind: Deployment
metadata:
  name: voltrico-user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: voltrico-user-service
  template:
    metadata:
      labels:
        app: voltrico-user-service
    spec:
      containers:
        - name: voltrico-user-service
          image: eloritz/voltrico-user-service:latest
          ports:
            - containerPort: 3002
          envFrom:
            - configMapRef:
                name: voltrico-user-service-config
            - secretRef:
                name: voltrico-user-service-secrets
