apiVersion: apps/v1
kind: Deployment
metadata:
  name: voltrico-order-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: voltrico-order-service
  template:
    metadata:
      labels:
        app: voltrico-order-service
    spec:
      containers:
        - name: voltrico-order-service
          image: eloritz/voltrico-order-service:latest
          ports:
            - containerPort: 3005
          envFrom:
            - configMapRef:
                name: voltrico-order-service-config
            - secretRef:
                name: voltrico-order-service-secrets
